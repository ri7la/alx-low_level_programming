#include "main.h"

/**
 * _abs - Compte the absolute value of an integer
 *
 * @i: input number
 *
 *
 * Return: absolut value
 */
int _abs(int i)
{

	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-1 * i);
	}
}
